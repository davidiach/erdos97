# Provenance and claim scope

Research date: 9 September 2026.

The GitHub connector inspected `davidiach/erdos97` main at
`047d05149382e48b602b292df4b8fc9e2da560bb`. This is the repository baseline
used here, not a claim that all incoming research has been independently
accepted. No repository write, PR, accepted-bound change, or status transition
was performed.

## Inputs

`inputs/internal_support.zip` is the original delivered internal-support
packet, byte-for-byte. Its SHA256 is

```
dfd032636763cf67c45338c22f24334eb9e5b17367a0de57b00eb074cdfea0e1
```

It contains the preceding cap-closure packet and the earlier co-design packet
as nested hash-bound archives. Their files, evidence, historical statements,
and scope qualifications have not been rewritten. The new proof uses the
old-point ceiling, 42 slot domains, and 123 slot-to-slot necessary edges;
these are reconstructed, not accepted from summary prose.

`inputs/prior_coarse_probe.json` is the preceding mixed-lens packet's exact
but incomplete two-free diagnostic. That probe checked 4,500 cases, rejected
1,824 and retained 2,676. Its explicitly omitted conditions are retained in
its own report. Those graph survivors were not geometric candidates.

The known recurrence and named witnesses were also read from
`incoming/unbounded-six-exception-family-2026-09-06/README.md` at the repository
baseline above. The 15-point control in this packet is a finite instance of
that existing recurrence. Its strict convexity and full multiplicity census
are checked directly here; no accepted arbitrary-length theorem is assumed.

## New work

The new maintained files implement two-free continuous-domain bounds,
common-radius covers, an exhaustive witness-closed subset search, and exact
strict-distance cancellation checks. The new proof excludes exactly two
internally supported new vertices for the fixed nine-point seed. Together
with the retained earlier results, an all-rich repair requires at least three.

The 15-point rich-cap control has exactly three internally supported new
vertices but leaves all six old exceptions good. It does not settle the
all-rich repair problem, show optimality for complete repairs, or provide an
Erdős97 counterexample.

Historical discovery scripts under `exploratory/` are preserved as executed,
including mounted paths. Their output records are sufficient to rebuild the
partition with `build_certificate.py`. Mathematical acceptance does not rely
on trusting their success flags: `verify.py` and `oracle.py` independently
reconstruct and reject every leaf. `discover.py` is a portable fresh search
entry point, and explicitly reports a reached guard as unresolved.

## Verification categories

The primary checker uses exact Q(sqrt(721)) arithmetic. The separate checker
uses a different radical representation, rational square-root enclosures,
Minkowski-difference geometry, set-based search, and a different equality
quotient. The two control checkers use respectively exact nested radicals,
and polynomial identities plus dyadic enclosures.

Different implementations in one session are not external independent review.
No Lean formalization, published-novelty review, or repository-wide CI is
claimed. The written geometric reductions remain expert-review obligations.
No global problem-status assertion is inferred from these calculations.

`validation.json` records actual command exit codes and the root Python
source hashes before and after the final run. `tools/package.py` checks those
hashes before constructing a manifest and ZIP. Packaging metadata uses a
fixed research-date ZIP timestamp for reproducibility; it is not a record
of the individual command execution times.
