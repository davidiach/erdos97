# Provenance and claim ledger

## Repository snapshot

The connected GitHub read of `davidiach/erdos97/branches/main` returned
`047d05149382e48b602b292df4b8fc9e2da560bb`, the merged PR #942 snapshot.
No repository write, PR, accepted-claim update, or repository-wide CI run was
performed. Standalone packet validation is not repository-wide verification.

Relevant repository sources inspected at that commit include
`src/erdos97/danzer18_doubling.py`,
`scripts/check_danzer18_base_nonagon.py`, and the indexed base-apex,
crossing-bisector, and failed-doubling records. The numerical initializer is
not promoted to an exact construction. The pair-capacity principle is reused
from the existing elementary geometry; its copy-budget application is stated
with its own hypotheses and no novelty claim.

## Delivered mathematical inputs

The preceding user-delivered archive is retained byte for byte as
`inputs/cap_closure.zip`, SHA256
`0a81018d0cb791362dda69506bc5044b260ef8d21d8627820dcf5f86234cc33c`.
It includes the earlier code-design packet in its own inputs. The old seed,
84-triple ceiling, 42 slots, 123-edge graph, and zero-free cap classification
are inherited results, explicitly replayed rather than renamed as new.

The new work consists of the full single-internally-supported-vertex
exclusion, the positive control, the symmetry-free copy-budget application,
and the moving-coordinate experiments and their exact fixed-pattern audits.
The numerical scans are not mathematical exclusions. The separate exact
quadrilateral certificates establish precisely their stated fixed-pattern,
fixed-order contradictions.

## Replays and failures

`data/prior_replay_current.json` is the fresh inherited replay from this
session, not a copied prior publication claim. Its child commands all returned
zero and all 30 + 31 inherited tests passed. Its captured stderr contains an
unrelated environment spreadsheet-startup warmup failure; this did not stop
those commands. The raw warning is retained rather than silently removed.
The later new exact commands used Python's `-S` option to avoid that startup
hook and need no third-party packages.

The new 40-test suite includes both complete one-free implementations.
A final targeted repeat of its pattern-audit test checks the completed
filtered scan after its data were written. Repeating that test is not counted
as an additional distinct test. The command outputs and final audit are in
`data/` and summarized in `validation.json`.

The moving-pattern discovery failures, including a short solver timeout and
a duplicate-new-cut programming guard, are retained and described in
`exploratory/RESULTS.md`. The completed repaired scan is a fresh invocation;
no result JSON was fabricated for the interrupted invocation. The original
interactive pattern's retained optimizer metadata do not include a complete
solver log or reconstruct its wall time.

## Independent-checking limits

The second one-free checker imports the prior separate radical algebra but
neither the new primary checker nor its primary arithmetic/geometry code.
It constructs distance minima and quadratics differently. Pattern
certificates have a separate graph-component arithmetic check. The rational
control also has integer homogeneous-coordinate checking.

All implementations were written in this research workflow. This is not
external expert review, a formally verified kernel, a Lean proof, a novelty
assessment, or a solution of Erdős97. The full geometric reductions remain
inspectable review obligations in `proofs.md`.
