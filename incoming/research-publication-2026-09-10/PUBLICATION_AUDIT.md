# Publication audit — September 10, 2026

This audit records the export-preparation session. Its statements about missing
tools, unrun repository gates, and remote publication refer to that session.
Subsequent repository validation is recorded in [INTEGRATION.md](INTEGRATION.md).
The original mathematical scope and missing-source qualifications still apply.

## Immutable inputs and useful scope

All 168 original files are preserved. The complete product-cycle ZIP has 133
members; 19 general-star files and 14 global-bridge files came from separate
attachments, not complete earlier archives. Two loose records are retained for
provenance. Nine separately attached copies are identical to already retained
members and are linked in provenance rather than duplicated again. The original
field helper also appears in two historical snapshots to preserve their import
structure. Nothing is reconstructed and labelled as original missing code.

The ZIP SHA256 is
`df1e4fd01729994508086c7660491fdd8dd7fd132f1133da19937914989e975e`.
Its original manifest has 132 entries, excluding the manifest itself. Fresh
`package.py --check` passes. All five historical publication-core files match
PR #944 at `832fe620583488b5c4a1169c75b486b2debf4bad` by Git blob hash.

## Corrections to historical statements

The product snapshot's `repro.md` introductory paragraph still says 35 + 12
or 47 tests. Its README, executable replay and fresh unittest logs establish
38 + 12 = 50 original tests. The obsolete paragraph is retained as historical
text, not adopted as the current instruction. Use the parent publication commands.
The later phase-filter section supersedes the earlier paragraph saying phase
filter integration is still next work.

The earlier `gabriel_defect.md` describes `verify/gabriel_defect.py` and
`verify/check_defect_algebra.py` as available. Neither was delivered. The prior
response explicitly said the consolidated regression scripts were unfinished.
The inequality, slack identity and cubic-growth argument are therefore retained
as a **written proof candidate**, not as a newly replayed general theorem.
The six-point degree-lifting control has its actual delivered exact checker and
passes independently of those missing scripts.

The general-star export has `check_angles.py` but lacks the `two_stars.py` and
`angle_minconflicts.py` generators named in reports. Fresh sparse replay proves
that all stored certificate identities and positive angle vectors are correct;
it does not regenerate the type enumeration, validate completeness/symmetry
reduction, or reproduce the stochastic discovery jobs. Events and learned-record
copies agree. The 799 and 487 recorded systems are distinct within their own
runs. Counts from different corpora should not be interpreted as a single
all-configuration coverage theorem.

The higher-product 300-case hull probe, 20 supplier-subset runs, 45-orbit
abstract model and changed-witness hole-walk records are retained at their
historical evidence levels. Missing search dependencies and hard-coded mounted
paths remain visible. They are not included in the fresh exact acceptance totals.
The original `circ24.json` is only `[]`, with no coordinates, enumeration source,
precision or completeness record. Prior conversational counts of 1,863 centers
and 2,024 triples cannot be established from that attachment. It is not an
exact cap-completion obstruction and is not used as proof evidence.

Historical #942 replay reports are retained as earlier executions. This import
does not claim another full #942 source replay from the incomplete attachments.

## Fresh mathematical evidence checks

The complete latest packet runs its own exact replayers, positive controls and
50 tests in isolation. The new attachment checker reconstructs the earlier
triangle-angle and isosceles equations sparsely. It checks exactly the existing
certificate contract: equality contradictions have nonzero constant, strict
angle contradictions have a nonempty positive contribution and nonpositive
constant, and positive vectors satisfy every exact equation and strict angle.
It does not infer feasibility from a solver status. Sparse and original dense
constraint lists are compared over several small sizes, and both checkers replay
representative positive/negative controls and one full 20/30-label certificate.

Earlier rational 6/13/21-point controls and quadratic-field 9/17-point controls
are rechecked from coordinates at all radii with all global supporting signs.
The exact old-triple circumcenter scan is rerun for its nine-point seed and its
full report matches: 84 triples, 37 distinct centers, no convexly addable new
center. This is the specified finite universe, not arbitrary additions. The
scan reuses its historical exact hull algorithm; this is not external review
or a new independent all-real geometric reduction.

## Repository integration

Only the new dated incoming directory and its navigation entry are intended
changes. Snapshot files have text conversion disabled to preserve hashes,
including historical logs. Existing root workflows, accepted claims and
certificates remain unchanged. The packet-local collection/Ruff exclusions
cover immutable research inputs, not maintained importer code. Every retained
original test remains reachable through the artifact replay.

No new PR has been opened in this preparation turn. The account's admin access
is distinct from this turn's exposed actions, which contain no write method.
Direct `git ls-remote` failed DNS; no CLI credential helper or token was available.
The prepared publisher is an unexecuted fallback, not proof of remote publication.

Unrun required gates: `make verify-fast`, `make verify-artifacts`,
`python -m ruff check .`, and Python 3.10/3.11 compatibility collection.
No full repository checkout or local Ruff was available. No Lean changes.
Independent mathematical/code review remains required for claim acceptance.

## Lossless storage of one historical script

The original `global-bridge-stress-2026-09-09/search/angle_hole_walk.py` has two
trailing spaces on historical lines 13 and 53. Its exact original bytes are
retained in `snapshots/global-bridge-stress-2026-09-09/search/angle_hole_walk.py.zip`
as the single member `angle_hole_walk.py`. It is not rewritten, and no whitespace
check is disabled. The parent provenance and replay verify the decoded original
bytes; the transport manifest also binds the physical ZIP. Thus 168 original
source files are preserved, with one stored losslessly as a ZIP member. This
numerical discovery script is not imported by the exact replay.
