# Import-preparation record — 2026-09-10

This is publication work, not a new research round. Read the snapshot logs for
their original hypotheses, searches and failure mechanisms.

The live main ref was read through the connector, as were PR #944, AGENTS.md,
CONTRIBUTING.md and the latest two PR records. #943 is merged; #944 remains a
draft at 832fe620583488b5c4a1169c75b486b2debf4bad. All 133 ZIP members pass their
original manifest, and all separately attached duplicates match byte-for-byte.

The immutable import has 168 files. A fresh isolated replay verifies the full
latest packet, earlier exact coordinate controls, old-triple scan and stored
angle certificates. The sparse attachment verifier is new publication code;
its identities are cross-checked against the delivered dense implementation.
All 19 new defensive tests pass. Fresh reports remain separate from snapshots.

No numerical optimizer was run. No missing historical source was recreated or
passed off as delivered. The stale test counts and absent Gabriel replay files
are documented in PUBLICATION_AUDIT.md. Nothing in this import changes accepted
mathematical status or supplies the missing unrestricted extraction theorem.

GitHub writes were not exposed despite successful reads/admin permission.
Direct Git failed DNS. No new remote branch, commit or PR was created. A
portable, guarded publisher accompanies the prepared import for execution in
an authenticated full checkout; its remote publishing path was not run here.
