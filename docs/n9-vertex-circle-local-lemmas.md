# n=9 Vertex-circle Local Lemma Extraction

Status: `REVIEW_PENDING_LOCAL_LEMMA_CANDIDATE`.

This note extracts reusable local obstruction candidates from the
review-pending `n=9` vertex-circle template packets. It does not prove `n=9`,
does not claim a counterexample, does not independently review the exhaustive
checker, and does not update the official/global status of Erdos Problem #97.

The companion checker is:

```bash
python scripts/check_n9_vertex_circle_local_lemmas.py --assert-expected --json
```

It scans only the stored local-core template packets:

- `data/certificates/n9_vertex_circle_self_edge_template_packet.json`
- `data/certificates/n9_vertex_circle_strict_cycle_template_packet.json`

It does not enumerate all `n=9` selected-witness assignments.

## Shared kernel

Let `P` be a strictly convex polygon and let `S_i` be selected witness rows. If
four witnesses of a row `S_c` appear in angular order

```text
a, b, x, d
```

around the hull vertex `c`, then the chord interval for `{a,d}` strictly
contains the chord interval for `{a,b}`. Since all four witnesses lie on the
circle centered at `c`, vertex-circle chord monotonicity gives

```text
D_ad > D_ab.
```

More generally, if a row sees witnesses in order `a,b,c,d`, the chord interval
for `{a,d}` strictly contains the chord interval for `{b,c}`, so

```text
D_ad > D_bc.
```

The second ingredient is the selected-distance quotient: each row identifies
the four squared distances from its center to its selected witnesses. A local
obstruction appears when vertex-circle strict inequalities become either a
reflexive strict edge or a directed strict cycle after quotienting.

## Lemma A: shared-endpoint nested self-edge

Suppose distinct labels `c,a,b,x,d` satisfy:

```text
{a,b,x,d} subset S_c,
```

and the angular order around `c` is `a,b,x,d`, up to reversal. Suppose also:

```text
{c,a} subset S_d,
{c,b} subset S_a.
```

Then no strictly convex selected-witness realization satisfies these hypotheses.

Proof sketch. The row `c` gives the vertex-circle strict inequality

```text
D_ad > D_ab.
```

The row `d` gives `D_ad = D_cd`, the row `c` gives `D_cd = D_ac`, and the row
`a` gives `D_ac = D_ab`. Hence the selected-distance quotient forces

```text
D_ad = D_ab,
```

contradicting `D_ad > D_ab`.

The checker finds this exact local pattern in the T02 self-edge packet:

```text
families: F01, F04, F08, F14
covered assignments: 40
```

The listed local cores have no two-circle cap violation and no
crossing-bisector order violation before the vertex-circle quotient step.

## Lemma B: nested-spoke quotient self-edge

Suppose a selected row has witnesses in angular order

```text
a, b, c, d
```

and suppose the selected-distance quotient generated by the supplied local
rows identifies the ordinary pair distances:

```text
D_ad = D_bc.
```

Then the local core is impossible. The vertex-circle chord nesting at the row
center gives:

```text
D_ad > D_bc,
```

which becomes a reflexive strict edge after quotienting.

The direct two-row special case proposed in solver output 3 would use

```text
{b,d} subset S_a,
{a,c} subset S_b,
```

to force `D_ad = D_bc`. The current packet scan finds no exact direct two-row
instances of that special case. It does find the more general quotient version
in the following self-edge families:

```text
families: F02, F03, F06, F09, F10, F11
covered assignments: 96
strict-edge instances: 8
```

Those instances use longer selected-distance quotient paths rather than the
two-row shortcut.

## Lemma C: T10/F12 two-edge strict cycle

The T10/F12 local core is:

```text
0 -> {1,2,5,6}
3 -> {0,1,4,6}
6 -> {1,3,4,7}
8 -> {0,3,6,7}
```

At center `8`, the witnesses `0,3,6,7` occur in angular order, so

```text
D_06 > D_03.
```

Rows `3` and `6` identify:

```text
D_03 = D_36 = D_16.
```

At center `3`, the witnesses `4,6,0,1` occur in angular order, so

```text
D_16 > D_01.
```

Row `0` identifies:

```text
D_01 = D_06.
```

Combining these gives the directed strict cycle

```text
D_06 > D_16 > D_01 = D_06.
```

The checker finds this exact strict-cycle local core in:

```text
family: F12
covered assignments: 18
```

Again, the local core has no two-circle cap violation and no crossing-bisector
order violation before the vertex-circle quotient step.

## Scan summary

The current local-lemma scan covers these review-pending template-packet
families:

```text
F01, F02, F03, F04, F06, F08, F09, F10, F11, F12, F14
```

The de-duplicated assignment coverage is:

```text
154 of the 184 n=9 pre-vertex-circle assignments
```

This is useful proof-mining coverage, not an `n=9` completeness proof. The
remaining local families still need proof-facing extraction, especially the
T03/T04 self-edge packets and the T11/T12 strict-cycle packets.
